[package]
name = "lattice-analyze"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "lattice-analyze"
path = "src/main.rs"

[dependencies]
lattice-core = { path = "../lattice-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
